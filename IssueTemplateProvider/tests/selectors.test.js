// import { selectIssueTemplateProviderDomain } from '../selectors';

describe('selectIssueTemplateProviderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
