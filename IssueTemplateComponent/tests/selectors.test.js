// import { selectIssueTemplateComponentDomain } from '../selectors';

describe('selectIssueTemplateComponentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
