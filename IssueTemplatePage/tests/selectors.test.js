// import { selectIssueTemplatePageDomain } from '../selectors';

describe('selectIssueTemplatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
