/*
 * CategorySelector Messages
 *
 * This contains all the text for the CategorySelector component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CategorySelector';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CategorySelector component!',
  },
});
